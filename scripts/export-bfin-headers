#!/usr/bin/env bash
set -euo pipefail

if [[ $# -ne 1 ]]; then
	echo "usage: $(basename "$0") DEST_DIR" >&2
	exit 1
fi

SRC=/opt/bfin/bfin-elf/include
DEST=$1

if [[ ! -d "$SRC" ]]; then
	echo "error: headers not found: $SRC" >&2
	exit 1
fi

mkdir -p "$DEST"
cp -a "$SRC"/. "$DEST"/
